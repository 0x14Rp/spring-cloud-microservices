logging:
    level:
        org:
            hibernate:
                SQL: debug
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: sasa
        username: root
        url: jdbc:mysql://localhost:3306/db_microservices_test?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true